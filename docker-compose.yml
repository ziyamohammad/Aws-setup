version: '3.9'

services:
  frontend:
    image: mohammadziya1/frontend-image
    container_name: frontend
    restart: always
    # ports:
    #   - "4000:80"

  backend:
    image: mohammadziya1/backend-image
    container_name: backend
    depends_on:
      - mongo
    env_file:
      - ./backend/.env
    restart: always
    # ports:
    #   - "8000:5000"

  mongo:
    image: mongo
    restart: always
    volumes: 
     - mongo-data:/data/db

  nginx:
    image: nginx
    container_name: nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - frontend
      - backend


volumes: 
  mongo-data:
     